version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.14"

# Calango is a C++ application — nothing to pip-install from the repo root;
# only the Sphinx toolchain is needed.
python:
  install:
    - requirements: docs/sphinx/requirements.txt

sphinx:
  configuration: docs/sphinx/source/conf.py
